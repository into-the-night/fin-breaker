# fin-breaker: Multi-Agent Finance Assistant

## Setup & Deployment

1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Run Streamlit app: `streamlit run streamlit_app/app.py`
